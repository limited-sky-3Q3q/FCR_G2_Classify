# FCR 预测模型 Web 应用 - 部署说明

## 📁 部署文件结构

```
分类预测网页/
├── app.py                                    # Streamlit 主应用文件
├── requirements.txt                          # Python 依赖包
├── packages.txt                              # 系统依赖包
├── README.md                                 # 项目说明
├── .streamlit/
│   └── config.toml                           # Streamlit 配置
└── 各模型最优参数（可复现）/
    └── fcr_web_model.pkl                     # 训练好的模型文件
```

## 🚀 本地运行

### 1. 安装依赖
```bash
cd 分类预测网页
pip install -r requirements.txt
```

### 2. 运行应用
```bash
streamlit run app.py
```

浏览器会自动打开 `http://localhost:8501`

---

## ☁️ Streamlit Cloud 部署（推荐，免费）

### 步骤 1: 创建 GitHub 仓库

1. 在 GitHub 上创建一个新的公开仓库（Public）
2. 将 `分类预测网页` 目录下的所有文件上传到仓库

### 步骤 2: 使用命令行上传（可选）

```bash
cd "分类预测网页"
git init
git add .
git commit -m "FCR预测模型Web应用"
git branch -M main
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main
```

### 步骤 3: 在 Streamlit Cloud 部署

1. 访问 [Streamlit Cloud](https://share.streamlit.io)
2. 使用 GitHub 账号登录
3. 点击 "New app"
4. 填写配置：
   - **Repository**: 选择你的仓库
   - **Branch**: `main`
   - **Main file path**: `app.py`
5. 点击 "Deploy"

等待 2-3 分钟，应用即可访问。

---

## 📋 requirements.txt 依赖说明

- streamlit - Web 框架
- scikit-learn - 机器学习模型
- pandas - 数据处理
- numpy - 数值计算

## ⚠️ 注意事项

1. **仓库必须设为 Public（公开）**，Streamlit Cloud 不支持私有仓库免费部署
2. 模型文件 `fcr_web_model.pkl` 约 2-3MB，确保上传完整
3. 如果部署失败，检查 `requirements.txt` 是否包含所有依赖
4. Streamlit Cloud 会自动检测并安装依赖

## 🔍 故障排查

### 问题 1: 模型加载失败
- 检查 `fcr_web_model.pkl` 是否正确上传
- 确保路径为 `各模型最优参数（可复现）/fcr_web_model.pkl`

### 问题 2: 依赖安装失败
- 确保 `requirements.txt` 文件正确
- 检查包版本是否兼容

### 问题 3: 应用无法启动
- 查看部署日志获取详细错误信息
- 确认 `app.py` 文件格式正确（无语法错误）
